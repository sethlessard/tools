import { describe, test } from "mocha";

describe("t00ls.vscode/commands/git/changeGitMode/domain/usecases/changeGitMode", () => {

  test("It should be able to change the Git mode to 'Normal'");
  
  test("It should be able to change the Git mode to 'Local'");
});
