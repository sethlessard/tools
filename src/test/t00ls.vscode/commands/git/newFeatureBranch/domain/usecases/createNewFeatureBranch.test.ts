import { describe, test } from "mocha";

describe("t00ls.vscode/commands/git/newFeatureBranch/domain/usecases/createNewFeatureBranch", () => {
  test("It should be able to create a new feature branch");
  test("It should throw an error if the feature branch already exists.");
});
