import { describe, test } from "mocha";

describe("t00ls.vscode/commands/git/newProductionReleaseBranch/domain/usecases/createProductionReleaseBranch", () => {
  test("It should be able to create a production release branch.");
  test("It should throw an error if the production release branch already exists.");
});
