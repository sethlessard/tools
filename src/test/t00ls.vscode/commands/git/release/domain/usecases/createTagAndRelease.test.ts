import { describe, test } from "mocha";

describe("t00ls.vscode/commands/git/release/domain/usecases/createTagAndRelease", () => {
  test("It should be able to create a production tag and finally release");
  test("It should throw an error if a tag for the release has already been created in the repository");
});
