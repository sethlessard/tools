import { describe, test } from "mocha";

describe("t00ls.vscode/commands/git/syncRepo/domain/usecases/syncRepository", () => {
  test("It should be able to sync a repository.");
  test("It should throw an error if there is a merge conflict.");
});
